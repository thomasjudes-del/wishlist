export function AboutPage() {
  return (
    <section className="content-card" aria-labelledby="about-title">
      <h1 id="about-title">About this starter</h1>
      <p>
        The repository is intentionally limited to application scaffolding so the
        complete Wishlist product can be built from the next set of requirements.
      </p>
    </section>
  );
}
