export function HomePage() {
  return (
    <section className="hero" aria-labelledby="home-title">
      <p className="eyebrow">Project foundation</p>
      <h1 id="home-title">Wishlist is ready for product direction.</h1>
      <p>
        This starter application includes React, TypeScript, routing, and an
        Appwrite client configuration. Product requirements and visual direction
        will be supplied next.
      </p>
    </section>
  );
}
